{
  "//name": "El paquete se llama chuno-cli y no chuno porque npm rechaza el nombre corto: su filtro de similitud lo considera demasiado parecido a 'hono', que es justamente la librería sobre la que corre este Worker. El ejecutable SÍ se llama chuno (ver bin), así que dentro del paquete todo sigue igual.",
  "name": "chuno-cli",
  "version": "0.1.1",
  "type": "module",
  "description": "El asistente que se acuerda de lo que le prometiste a tu cliente.",
  "bin": {
    "chuno": "cli/chuno.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/diegoalvarez0918-beep/chuno.git"
  },
  "//files": "Lista blanca, no lista negra. Publicar en npm es público e irreversible: un .dev.vars que se escape no se recoge, se rota. Enumerar lo que viaja es la única forma de saber qué viaja. src/ y wrangler.jsonc van porque el instalador despliega DESDE el paquete descargado; public/ va porque wrangler falla si el directorio de assets no existe.",
  "files": [
    "cli/",
    "src/",
    "public/",
    "wrangler.jsonc",
    "tsconfig.json",
    "README.md"
  ],
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "db:local": "wrangler d1 execute chuno --local --file=src/db/schema.sql",
    "db:remote": "wrangler d1 execute chuno --remote --file=src/db/schema.sql",
    "seed:local": "wrangler d1 execute chuno --local --file=src/db/seed.sql",
    "seed:remote": "wrangler d1 execute chuno --remote --file=src/db/seed.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
